import { FlexProps } from "@mpkelly/siam";
export interface NotFoundProps extends FlexProps {
    icon: string;
    labelKey: string;
}
export declare const EmptyView: (props: NotFoundProps) => JSX.Element;
