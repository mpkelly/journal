import { ReactNode } from "react";
import { FlexProps } from "@mpkelly/siam";
export interface EditorPageEditorProps extends FlexProps {
    children: ReactNode;
}
export declare const EditorPageEditor: (props: EditorPageEditorProps) => JSX.Element;
